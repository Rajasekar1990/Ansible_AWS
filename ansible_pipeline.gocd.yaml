format_version: 10
pipelines:
  Ansible_FT_AWS:
    group: defaultGroup
    label_template: ${COUNT}
    lock_behavior: unlockWhenFinished
    display_order: -1
    environment_variables:
      Playbook_name: ft_playbook_v0.7.yml
      Archive_flag: OFF
      File_loc: C:\src_loc\
      Tmp_loc: /home/ubuntu/ansible/tmp/
      Target_loc: C:\{{ inventory_hostname }}\
      File_size: 2k
    secure_variables:
    materials:
      git-e912034:
        git: https://github.com/Rajasekar1990/Ansible_AWS.git
        username: Rajasekar1990
        shallow_clone: false
        auto_update: true
        branch: main
        password: AES:tyM62jnM4IMK9AMLfWYHmg==:l8lHguVKMszpxWcJoM3ayA==
        # destination: gocd_ansible
    stages:
    - Agent_Check:
        fetch_materials: true
        keep_artifacts: false
        clean_workspace: false
        approval:
          type: manual
          allow_only_on_success: false
        jobs:
          Agent_validate:
            timeout: 0
            tasks:
            - exec: 
                arguments:
                - c
                - echo 'Ready to execute ansible playbook ${Playbook_name} ${Archive_flag} ${File_loc}'
                command: /bin/bash
                run_if: passed
            - exec:
                command: pwd
                run_if: passed
    # - File_Transfer:
    #     fetch_materials: true
    #     keep_artifacts: false
    #     clean_workspace: false
    #     approval:
    #       type: success
    #       allow_only_on_success: true
    #     jobs:
    #       Run_FT_Playbook:
    #         timeout: 0
    #         artifacts:
    #         - build:
    #             source: ansible_log*
    #             destination: ''
    #         tasks:
    #         - exec: 
    #             arguments:
    #             - -c
    #             - cd /
    #             command: /bin/bash
    #             # working_directory: gocd_ansible
    #             run_if: passed
    #         - exec:
    #             arguments:
    #             - -c
    #             - cd /home/ubuntu/ansible
    #             command: /bin/bash
    #             # working_directory: gocd_ansible
    #             run_if: passed
    #         - exec:
    #             arguments:
    #             - -c
    #             - ansible-playbook -i inventory ${playbook_name} -vv
    #             command: /bin/bash
    #             # working_directory: gocd_ansible
    #             run_if: passed